#ifndef UTILS__H
#define UTILS__H
#include <fstream>
#include <string>

//Полезни функции

int digits(int);
bool isOdd(int);
std::ostream& printMany(const char*, size_t, std::ostream&);
bool isSpaceBetweenWords(std::string, size_t);



#endif 